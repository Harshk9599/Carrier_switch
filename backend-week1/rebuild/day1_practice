// creating server just basic

const express = require("express");

const app = express();

const PORT = 3000;

app.get("/about", (req, res) =>{
    res.json({ info: "My first backend server."});
});


app.listen(PORT, () =>{
    console.log(`Server is listening at ${PORT}`);

});





